# ProjectToDo (Flask)

Prosta aplikacja Flask z logowaniem/rejestracją i feedem postów.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
sql
Skopiuj kod

Zapisz i wypchnij:
```bash
git add README.md
git commit -m "Add README"
git push
